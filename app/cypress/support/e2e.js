// This is the Cypress support file.
// It is automatically loaded before your test files.
// Use this file to define custom commands, import utilities, or set up global configuration for your Cypress tests.

// Example: Add a custom command for logging in
// Cypress.Commands.add('login', (email, password) => {
//   cy.get('input[name="email"]').type(email);
//   cy.get('input[name="password"]').type(password);
//   cy.get('button[type="submit"]').click();
// });

// You can now use cy.login('user@example.com', 'Test1234!') in your tests.